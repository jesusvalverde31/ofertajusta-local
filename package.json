{
  "name": "ofertajusta-local",
  "version": "1.0.0",
  "private": true,
  "description": "Comparador local y explicable de ofertas de empleo.",
  "scripts": {
    "start": "node iniciar.cjs",
    "check": "node --check server.cjs && node --check storage.cjs && node --check engine.js && node --check iniciar.cjs && node --check public/app.js && node --check public/engine.js",
    "test": "node --test --test-isolation=none test/server.test.cjs"
  },
  "engines": { "node": ">=24" }
}
